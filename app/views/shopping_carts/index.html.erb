<p>The contents of your shopping cart are as follows:</p>

<ul>
<%shopping_cart_get_tickets().each do |k,v| %>
  <li><%=TicketType.find(k).event.name%>, <%=TicketType.find(k).event.start_date.in_time_zone()%>, <%=TicketType.find(k).ticket_zone.zone%>, <%=TicketType.find(k).price%>, <%=v%></li>
<%end%>
</ul>

<%= link_to 'Back', :back %>

<!-- TODO: remove items from cart -->
<!-- TODO: button for placing order -->